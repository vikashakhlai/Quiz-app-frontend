import QuizEdit from '@/components/screens/quiz/QuizEdit';
import { NextPageAuth } from '@/shared/types/auth.types';

const QuizEditPage: NextPageAuth = () => {
	return <QuizEdit />;
};

export default QuizEditPage;
